export function BusinessName(props: { className?: string }) {
 return (
  <span
   className={`gradient-text text-2xl md:text-3xl font-bold tracking-[-0.12rem] md:tracking-tighter ${props.className}`}
  >
   DarlingtonSuite.
  </span>
 );
}
